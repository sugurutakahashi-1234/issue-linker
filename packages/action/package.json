{
  "name": "@issue-linker/action",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "default": "./dist/index.js"
    }
  },
  "private": true,
  "scripts": {
    "🔨": "========== Build & Type Check ==========",
    "build": "ncc build src/index.ts --license licenses.txt --out dist --transpile-only --no-cache && rm -rf dist/package.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "🧪": "========== Test ==========",
    "test": "bun test",
    "test:quiet": "bun test > /dev/null 2>&1 && echo '✅ Tests passed' || echo '❌ Tests failed'",
    "🎨": "========== Code Quality ==========",
    "fix": "biome check --write --unsafe .",
    "check": "biome check .",
    "📚": "========== Documentation ==========",
    "docs:deps-graph": "tsg --tsconfig ../../tsconfig.typescript-graph.json --LR --abstraction packages/cli --abstraction packages/core --md ../../docs/reports/dependencies/action.md",
    "🚀": "========== CI & Clean ==========",
    "ci": "bun run fix && bun run check && bun run build && bun run typecheck && bun run test:quiet",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@issue-linker/core": "workspace:*",
    "valibot": "^1.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.4",
    "@vercel/ncc": "^0.38.3",
    "@ysk8hori/typescript-graph": "^0.26.4",
    "typescript": "^5.9.2"
  }
}
